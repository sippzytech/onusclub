{
  "name": "stampdeck",
  "version": "0.0.1",
  "private": true,
  "description": "Stampdeck — multi-tenant SaaS for digital loyalty cards (Google/Apple Wallet).",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "db:migrate": "pnpm --filter @stampdeck/api run db:migrate"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
